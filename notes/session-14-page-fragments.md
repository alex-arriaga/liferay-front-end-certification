# Session 14. Page Fragments

They exist as individual items in a **Collection** (groups).

Developers can specify elements as editable using `<lfr-editable>`.

Folder structure for Fragment Collections:

```
collection-name/
```


